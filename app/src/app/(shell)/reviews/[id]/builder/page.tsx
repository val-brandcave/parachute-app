import { Stub } from "@/components/Stub";

export default function BuilderPage() {
  return (
    <Stub
      title="Workbook Builder"
      crumb="Technical Review"
      sprint="Sprint 2"
      note="Assemble and reorder workbook sections, show/hide content, drag in extra appraisal sections, and apply themes/fonts/risk-rating labels — per-review and as an org default. Coming in Sprint 2."
    />
  );
}
