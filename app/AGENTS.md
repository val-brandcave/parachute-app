<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Parachute v2 — Prototype Rules

This is the **Parachute v2** front-end prototype (Realwired's AI appraisal-review tool for banks).
It is a **clickable prototype, not production**: front-end + a mock service layer only, no real API,
no backend ownership. An engineering team builds the real product from this. Optimize for design
quality, clear patterns, and a clean hand-off — not premature backend concerns.

> The global `~/CLAUDE.md` (Windows rules — never redirect to `nul`, etc.) still applies.
> Product spec, IA, and design decisions live in `../docs/plans/` and project memory.

## Stack

- **Next.js (App Router) + TypeScript**, **Tailwind v4**, **Zustand**, **Framer Motion**, **Tabler icons** (`@tabler/icons-react`).
- Path alias `@/*` → `src/*`. Dev server: `npm run dev` (http://localhost:3000).

## Component architecture (atomic design)

Build UI as atoms → molecules → organisms → templates → pages. Decide the level first, then scope.

- **Atoms** (`src/components/atoms/`): primitives — `Button`, `Icon`, `Chip`, `Input`, `Tooltip`, `Modal`, `Logo`, etc. Stateless, props-driven, no business logic, no store access.
- **Molecules** (`src/components/molecules/`): small combinations of atoms — `Field`, `SegmentedControl`, `StatCard`, `NavItem`, `Breadcrumbs`, `PeriodPicker`, `ActionMenu`. Mostly props-driven.
- **Organisms** (`src/components/organisms/`): larger sections — `Sidebar`, `AppHeader`, `StatBar`, `ReviewTable`, `OrgCard`, dashboard widgets. **May** connect to stores/hooks.
- **Templates** (`src/components/templates/`): page scaffolding — `AppShell`, `PageHeader`.
- **Pages** (`src/app/.../page.tsx`): compose organisms/templates with real (mock) data via a page hook.

Each folder has a barrel `index.ts`. Import from the barrel (`@/components/atoms`), not deep paths.
Keep components small and single-purpose; extract shared patterns instead of duplicating. Props get sensible defaults; never hardcode user-facing copy that should be a prop.

## Styling & design tokens

- **All styling uses design tokens (CSS variables) defined in `src/app/globals.css`** and mirrored in `src/lib/tokens.ts`. **No arbitrary hex or pixel values** — use `--md-*` (color), `--d-*` (density spacing/scale), `--grad-*`, `--r` (radius).
- Component styles are **`.ui-*` / semantic classes in `globals.css`** — NOT CSS modules, NOT per-component `*.styles.ts`. Reuse existing classes; add new ones in the relevant section of `globals.css`.
- **Palette = "Navy & Petrol"** (revised Jun 2026 — supersedes the old "Slate & Teal"): primary navy `#10344C` (hover `#1B4F73`); accent **petrol `#2A6F7F`** (ink `#14505E`, tint `#DEEBEE`); **true-neutral greys** (no blue cast) — canvas `#F6F7F8`, surface `#FFF`, surface-1/hover `#F1F2F4`, surface-2 `#E9EBEE`, border `#E2E4E8`, hairline `#EDEEF1`. States: pass `#15834A`, flag `#C98A12`, fail `#D23F34`, critical `#7E1D1D`, info `#2D6CA6` (each with a tint).
  - **Primary CTAs are NAVY** (`--grad-primary`, a solid). **Petrol is for accents, links, focus, and AI cues only — never the default primary button.** **Selected/active *surfaces* derive from navy** (`--md-selected` / `--md-on-selected`), not petrol (nav active pill, `ui-chip--accent`, `ui-avatar--soft`).
  - **Full text scale:** primary `--md-on-surface` `#1A1D21`, secondary `--md-on-surface-v` `#565C64`, tertiary `--md-on-surface-t` `#868D96`, disabled `--md-on-surface-dis` `#AEB4BC`. Utilities: `.text-secondary` / `.text-tertiary` / `.text-disabled`.
  - **Flat treatment:** solid fills — **no gradient buttons, glows, or decorative texture.** Keep ONE restrained AI cue (flat petrol).
- **Type:** Schibsted Grotesk (display/headings) + **Inter** (body / UI / **numbers**). **NO monospace anywhere** — numbers/IDs use Inter with `font-variant-numeric: tabular-nums` (the `.mono` class is sans+tnum, despite its name), never a mono face. Don't load IBM Plex Mono or any mono via `next/font`.
- **Theme:** support **light + dark** via `:root[data-theme="dark"]`. **Never hardcode a color that breaks in the other theme — use tokens.** Overflow popovers / menus / cards must use `var(--md-surface)` (not `#fff`), or they render white with light text in dark mode. Theme-aware assets (logo) toggle via CSS on `[data-theme]`, **not** inline `display` overrides (inline styles beat CSS classes).
- **Density:** spacing/scale respond to `[data-density]` via `--d-*` vars; don't bypass with fixed paddings on layout-level elements.
- **Inputs: white in LIGHT mode (standing client rule); theme-aware in DARK mode** (revised Jun 19 2026 — the old "white in BOTH themes" read as focus-hungry white islands on dark cards; **flag the dark-mode change to Realwired** since white-both-themes was their explicit ask). Light mode keeps the white fill + pinned dark ink (`#1a1d21`) + `color-scheme: light` + the `:-webkit-autofill` white override on `.field`/`.ui-input`. **Dark mode overrides** (`:root[data-theme="dark"]`, scoped to `.field` + `.ui-input`/`.ui-textarea`): elevated dark fill (`--md-surface-2`), near-white ink (`--md-on-surface`), **`color-scheme: dark`** so native `<select>` option lists, date pickers and autofill render dark, a lighter select chevron, and a dark autofill box-shadow. Use **`background-color`** (not the `background` shorthand) in the dark override so the chevron image survives. **Field labels stack above the input** — not floating over the border (that style breaks on dark surfaces).
- Layout is **full-width** (no body max-width); content must reflow when the nav collapses. **Desktop-first** for now — don't break at smaller widths, but full mobile/tablet polish is a later phase.

## Icons

- Use **Tabler icons (`@tabler/icons-react`) via the `Icon` atom** (`name` from the curated map in `atoms/Icon.tsx`; default `strokeWidth` 1.75). Add new icons to that map. **No icon fonts (Material Icons removed), no ad-hoc inline SVG.** The one exception is **real brand marks** — `MicrosoftGlyph` / `YouConnectGlyph` in `atoms/BrandGlyph.tsx`, drawn with `currentColor` so they adapt to theme/button.

## Data layer (mock service layer)

The adapter pattern is the contract for hand-off (`../docs/plans/data-adapter-pattern-guide.md`):

```
Page → page hook → Zustand store → adapter → mock (localStorage) | api (stub)
```

- Each layer imports only the one below it. **Pages never touch the adapter; stores never skip it.**
- Stores own ID generation, timestamps, loading/error state. Seed data lives in `src/data/seed/`.
- Source is chosen by `NEXT_PUBLIC_DATA_SOURCE` (`mock` for the prototype). Wiring a real backend = implement `api-adapter.ts` + flip the env var, **zero UI changes**.

## State & React

- **Zustand** for shared/mutable state (review dispositions, prefs, queue). Local UI state stays in the component.
- Shared mutable state read/written by multiple screens must live in a store, never duplicated in component state.
- **No impure calls during render** (React 19): never call `Date.now()`/`Math.random()` in render — use a lazy `useState(() => …)` initializer or an effect.

## Motion (Framer Motion)

- Use for purposeful transitions (page/section reveals, the nav active-pill `layoutId`, modal/tooltip enter-exit).
- **Don't combine animated `x`/`y` with a manual centering `transform`** — Framer writes `transform`, overriding yours. Animate `opacity` (and position via `style`) instead.
- **Overlays (tooltips, menus, modals) render in a portal with fixed positioning** so ancestor `overflow: hidden` can't clip them.

## Accessibility

- All interactive elements keyboard-accessible with visible focus states (use `--ring-accent`).
- Inputs have associated labels; icon-only buttons get `aria-label`; decorative icons are hidden.
- Use semantic HTML (`nav`, `main`, `header`, `button`, `a`). Meaningful `alt` on images.
- Maintain WCAG AA contrast in **both** themes.

## Established UI patterns (reuse these)

- **Nav = Dashboard · Reviews · Templates · Settings.** The queue page is **"Reviews"** (renamed from "My Reviews" — it holds others' reviews too). Nav icons must be visually distinct: Reviews = list-check, Templates = template (not two near-identical document marks).
- **Page headers carry controls, not just titles.** A bare title that repeats the nav is redundant. Use the slim `.pagehead` band to host the page's primary controls: a greeting (Dashboard), a tab bar + search + filter + action (Reviews), or tabs (Settings). Keep titles only where they add information.
- **Tabs:** use the `Tabs` molecule (`.qtabs`/`.qtab`) for in-page partitions (queue tabs, settings sections). The active background is a **Framer `layoutId` pill that slides between tabs** (same motion language as the nav rail and `SegmentedControl`'s thumb); tabs also `layout`-animate in/out via `AnimatePresence` so hiding/showing them is smooth. `layoutId` is `useId`-scoped per instance. **Don't** put the active color on a static `.on` background — the sliding pill owns the fill (label/count sit above it via `z-index`).
- **Stats:** the `StatBar` organism — informational only, never a table filter; big number left, monochrome icon badge right (soft-grey tile + navy glyph; `alert` turns a tile red only when its value is non-zero), `InfoTip` on the label. Dashboard metric cards are **period-scoped by the period picker** EXCEPT genuinely point-in-time tiles (e.g. "Pipeline running"), which stay a live "now" count flagged with `live` (renders an inline pulsing "Live" marker next to the number). Card order follows the lifecycle: Intake triage → Pipeline running → Needs my action → Overdue → Completed.
- **Charts are bespoke SVG** (e.g. the dashboard "Review volume" combo: volume bars + a configurable overlay line), built with our tokens + CSS motion — no chart library. Pattern: period badge by the title, gridlines, date-based x-labels (tick-gated so they don't crowd), rich themed hover tooltip, a footer metric strip with vs-previous-period deltas, and chart config (overlay = turnaround/on-time/both/none) + actions in the `⋯` `ActionMenu`.
- **`ActionMenu`** (`⋯` overflow) supports plain actions, a section `header`, a `divider`, and checkable radio `selected` items (used for the chart overlay config). Use it for secondary/tertiary actions and small in-widget config.
- **Auth (login):** SSO-first with **progressive disclosure** — SSO options on top (**YouConnect is the single filled primary**; Microsoft + generic SSO are outlined), then a "Sign in with email instead" toggle that expands the email/password form inline (Framer Motion height/opacity). The expanded email **Sign in** is a *secondary* (outline `.sso`) button so YouConnect stays the only filled primary on the card. **One radius for all login controls (8px)** — don't reintroduce the global `.btn` pill here. Calm canvas (no gradient slab), theme-aware logo, white inputs.
- **RULE — every floating layer renders in a portal to `document.body` with `position: fixed`, positioned from the trigger's `getBoundingClientRect()`. NEVER `position: absolute` inside content.** This applies to *all* popovers — tooltips, `⋯` menus, dropdowns, select/multi-select menus, date pickers, hovercards. Reason: cards, table cells, sticky headers, and scroll containers use `overflow: hidden/auto`, which **clips any absolutely-positioned child** (this bug recurred in the version-history table and the reviews queue). Canonical implementations: `atoms/Tooltip.tsx` and `molecules/ActionMenu.tsx` — copy their shape for any new popover: `createPortal(…, document.body)`, fixed coords from the rect in a `useLayoutEffect` (runs before paint → no flash; park offscreen until measured), **flip/clamp to the viewport** (menu flips above when it would spill past the bottom; tooltip clamps by measured half-width), z-index **200** (the overlay tier), and **dismiss on outside-click (the handler must also exclude the portaled node, since it's not a DOM descendant of the trigger), Escape, scroll (capture phase), and resize.** `ActionMenu` was migrated to this Jun 19 2026 — before that it was `absolute` and got clipped.
- **Tooltips / menus / modals render in a portal** (fixed position) so ancestor `overflow:hidden` can't clip them. `Tooltip`/`InfoTip` (top or right), `ActionMenu` (⋯ overflow for secondary/tertiary actions). **`Tooltip` animates via Framer (subtle rise + fade + slight scale, spring)** using a **two-layer structure** — an outer wrapper owns the centering `transform`; an inner `motion` bubble animates `x/y/scale` — so we never fight Framer's transform write (the AGENTS Motion rule). The outer wrapper MUST keep `width: max-content` — `position:fixed` blockifies it, and without a width it fills the viewport so `translate(-50%)` offsets by half the screen. Props: `side`, `block`, `panel` (light rich-content surface), `disabled` (keep layout, suppress the tip — truncation-only tooltips), `compact` (minimal label chip — smaller, quick fade, no rise/scale; used for icon-button labels like ⋯ "More options" and the icon-only Download). **Two gotchas fixed Jun 18 2026:** (1) `.ui-tip` MUST be `display: inline-block` — as an inline span its background won't cover a block child, so the `panel` variant (which wraps a flex card) renders transparent. (2) The viewport edge-clamp uses the tip's **measured half-width** (in a `useLayoutEffect`), not a fixed margin — a fixed margin shoves narrow tips (e.g. compact "More options") off their trigger near a screen edge; any leftover clamp delta is fed to the arrow via `--arrow-dx` so it still points at the trigger.
- **Global overlays live in the shell, driven by a store.** The command palette is mounted in `AppHeader`; the Order stepper (`OrderModal`) is mounted once in `AppShell` and opened via `useOrderStore` from anywhere (button or palette). Don't mount per-page modals that need to open globally.
- **Command palette** (`⌘K`/`Ctrl+K`): search reviews + go-to pages + actions, keyboard-navigable. Add new destinations/actions there as routes grow.
- **Full-page multi-step flows** use the `StepperModal` organism. Left rail = **row-style steps** (label + trailing **donut indicator**: hollow petrol-capable ring → solid petrol disc + check when done; active row gets a navy-tinted `--md-selected` pill, no connectors, no descriptions). Footer = **Back on the left, Continue/Submit on the right** (no Cancel — the header X dismisses; no "Step X of N"). Donut completion is binary today (step done = passed). Pass `nextDisabled` to gate a step. `StepperModal` has an optional **`aside`** slot (a persistent right rail next to the step content) for future wizards — the Order flow does **not** use it. Order a review is the first; reuse for other wizards.
- **Order a review (`OrderModal`) — THREE steps** (revised Jun 18 2026; full spec in `../docs/plans/parachute-v2-early-specs.md` §1, decisions in `parachute-v2-ia-map.md` §8): **`Source · Configure · Summary`** (was 5 — type/reviewer/options collapsed into Configure; the final step's display label is **"Summary"**, its `ORDER_STEP` key stays `confirm`). Draft + step indices live in **`store/order.store.ts`** (`useOrderStore`, `ORDER_STEP`); the last-step index is `ORDER_STEP.confirm` — **don't hardcode the step number** in callers (dashboard "New from YouConnect", queue "Run"). **Source** = `SegmentedControl` From YouConnect (searchable `YcDelivery` inbox with NEW / IN QUEUE badges — IN QUEUE = a **second review**, allowed with an amber warning, never blocked) | Standalone upload (**upload-first**: dropzone hero → AI-autofilled editable property fields). **Each step's content is one white `.ord-card` on the modal canvas** (Configure's sub-sections are hairline-divided `.ord-sec`s). **Configure has a "Templates for this review" sub-section** (Jun 19 2026): an admin **checklist picker** (defaults to the org-default checklist — `ChecklistTemplate.isDefault`; an explicit pick = a per-order override, audited; `OrderDraft.checklistId`) and a read-only **inherited workbook-layout** row (the property's profile → that profile's default `WorkbookLayout`). The Summary step echoes both. Templates own the defaults (single source of truth — also settable in Settings → Review defaults). **NO progressive summary rail** (dropped Jun 18 2026 — it was reversed): steps 1–2 are full-width; the **Summary** step is the review surface — a **two-section** card: **Appraisal** (✎ Edit → Source) and **Configure** (✎ Edit → Configure; sub-grouped Review type · Reviewer & schedule · Options). **One Edit per editable step, not per field** (`onEdit(setStep)`), no ellipses. The footer submit is **🚀 Run** — a leading **rocket** icon (`submitIcon="rocket"` + `submitIconSide="left"` on `StepperModal`; not the default trailing check), label just **"Run"** (revised Jun 18 2026 — was "Run pipeline"). Org defaults use a **direct toggle** (no "Override…" disclosure); flipping it shows a "this override applies to this order only — audited" note. **Run pipeline** creates a `running` review via `useReviewsStore.addReview` and routes into it (decision #1). YC data is the `yc-deliveries.seed.ts` seed.
- **Single sources:** the signed-in user/org come from `lib/current-user.ts`; design values from `lib/tokens.ts` / CSS vars. Don't hardcode duplicates.
- **Theme-aware assets** (logo) toggle via CSS on `[data-theme]`, not inline `display`.
- **Review detail is ONE route `/reviews/[id]`.** Technical / Administrative is the **track**, and **Findings · Builder · Workbook** are the three **sub-views** of the Technical track — all driven by state via `ReviewContextBar`, never routes, never in breadcrumbs. Builder is its **own** sub-view (a full layout/section-authoring place — the earlier "fold into Workbook as Edit-mode" was reversed 2026-06-22; full record `../docs/plans/parachute-v2-review-details-spec.md`): Findings = decision surface, Builder = layout authoring, Workbook = clean compiled signable doc; org-default layout authoring still lives once in **Templates → Workbook Layout**. **The Administrative track has its OWN two sub-views `Attestations · Preview`** (built 2026-06-23; revises the earlier "Admin = no sub-nav" decision): Attestations = the focus-mode attestation surface (same `.fm-*` shell as Findings — list rail · focus pane · on-cite source — but a **Yes/No/N-A + Confirm** decision footer, reason required when the answer ≠ the AI's; coverage panel, Show filter, Confirm-routine bulk); Preview = the compiled, signable attestation **document** (the sibling of the Workbook, same `.wb-doc` shell + SHA-256 Sign). Admin has **no Builder** — the checklist/form layout is authored once org-level in **Templates → Compliance Checklist**, not per-review. Items are **derived from the org-default `ChecklistTemplate`** (single source of truth); the per-review AI pre-fill is a seeded `Attestation[]` zipped in **`admin.store.ts`** (parallel to Finding/`workspace.store`). **Context-bar layout (revised 2026-06-23, "Option 1") — TWO zones, not the old three-band stack:** (1) an **identity row** = address title + derived status chips beside it + `type · bank · loan#` line, with the **track as a right-aligned `SegmentedControl`** — rendered **only when `review.reviewTypes` has both tracks** (a lone track shows no switch); (2) the sub-views as the **`Tabs` molecule (sliding-pill, no icons)** — the house in-page pattern, replacing the old underline shape. The enclosed segmented switch (identity row) vs. free-standing pills (band below) keep the two nav levels visually distinct. Context-bar chips read **derived** state (`pipelineView`/`outcomeView`), never raw `status`. `/reviews/[id]/triage` is the only review sub-route. Don't add routed sub-pages under a review.
- **Reviews queue (`/reviews`) — state is DERIVED, never a synthetic status.** There is **no Status column**: a review's state reads from **Pipeline (phase) + Findings (outcome) + Type (kind)** together. All derivation lives in **`lib/review-lifecycle.ts`** (`pipelineView` · `outcomeView` · `nextActionView` · `lifecycleBucket` · `needsMyAction` · `isOverdue`) — reuse it; don't reinvent state logic in components. `ReviewStatus` = honest phases only (`intake · autorejected · running · in_review · returned · completed`). Columns (revised Jun 2026): **Property (address / `appraisalFirm · loan# · type`) · Reviewer (its OWN narrow, avatar-only column — `Avatar` + name on hover, NOT bundled into Property) · Type (TECH/ADMIN **chips** — spaced pills, can be both) · Pipeline (`PipelineTracker` = an animated **journey track**: filled/shimmering-active/idle segments + live stage name inline + hover tooltip of all S1–S5; **word-state badges for pre/post-pipeline** (revised Jun 18 2026): pre-order intake = a **"New"** info-pill (the leading glyph carries the source — YouConnect glyph for `source === "yc"`, `+` otherwise — and a **hover card** spells out "New from YouConnect … not yet run through Parachute"; revised Jun 18 2026, was the label "New from YC"); auto-rejected = a red **"Auto Rejected"** pill (NOT "Blocked at intake") with a **hover card** explaining it failed an automated compliance check and to open Triage. **Both word-state cards reuse the stepper's `panel` `.pipe-card`** (bold `hintTitle` header over a `.pipe-card-desc` body) via `hintTitle`/`hint` on `pipelineView`, so they look identical to the running/ready hover card — not the plain dark label tip; in_review = **`Ready`** (green success badge); completed = **`Completed`** (NOT "Done"). The **running** state renders its label as a petrol `.pipe-badge` (pulsing dot) **stacked ON TOP** of the S1–S5 track (never inline beside it). **Ready / Completed are badge-only** (revised Jun 18 2026 — the green `.pipe-badge--ready/--done` shows with NO track, since the track is only meaningful mid-flight; the hover card still lists every stage), whose hover card carries a **footnote** "Reviewed and signed by {reviewer}" (top-aligned check; passed from `ReviewTable` via the `footnote` prop). Word badge styling = `.pipe-word--badge` + a tone modifier (`--fail`/`--info`) for the tint) · Findings (worst-severity chip) · Due (a **neutral date** via `formatShortDate` + a trailing **urgency marker** — amber **clock** = due-soon, red **triangle** = overdue — whose **tooltip** carries the magnitude `Due in Nd`/`Overdue Nd`; on-track = date only; **completed shows its date with NO marker** (it's not overdue); `—` for auto-rejected (SLA paused). Marker shape+color, not color alone — a11y-safe) · Actions (one **merged right-aligned cell** = the derived primary action + `⋯` `ActionMenu`; no "Next action" header. The primary is **icon-only for every row** (revised Jun 18 2026 — was icon+label except Download): an **outline `.ui-btn--icon`** with the label in a **compact `Tooltip`** — Run (`rocket`) · Triage (`gavel`) · View (`eye`, running rows) · Review/Compile/Sign (`reviews`/`edit`, in_review) · Download (`download`, completed). The `⋯` carries a **"More options"** tooltip and its go-to item is **"Review"** (`reviews` icon, was "Open review"). **Only the `returned` wait-state renders NOTHING** (just the `⋯`): `running` is no longer quiet — it now has an **Eye → "View"** that opens the running review; the old "Running…"/"With appraiser" quiet labels are dropped)**. **Column interactions live in `ReviewTable` + `components/organisms/review-columns.ts`:** every header **except Actions is sortable** — click **cycles asc → desc → back to the smart default** (active shows a solid ↑/↓, idle headers a faint ⇅ on hover); sort state + comparators are in `useReviewQueue` (`sort` + `cycleSort`). A **column-config gear** (Tabler `columns`) sits in the **Actions header** and toggles column visibility — **Property + Actions are locked-on** (disabled in the menu); the other five are hideable; **session-only** (resets to all-visible on reload). The grid is **dynamic** (`gridTemplate(visibleCols)` inline on `.qcols`/`.qrow`). **Property's title + subtext ellipsise responsively** (`TruncText` — `ResizeObserver`) and show a tooltip **only when actually clipped** (`Tooltip disabled={!truncated}`), freeing width for the other columns. **Tabs partition by lifecycle stage** (revised Jun 18 2026): **All · Needs action · In pipeline · Intake · Completed**. **Auto-rejected lives under Needs action** (it needs the reviewer to triage now), so `lifecycleBucket` maps `autorejected → needs_action` and Intake = plain `intake` (awaiting order) only. The **"Sent back" (returned) tab + its example are removed pending client Q1** (`docs/plans/parachute-v2-client-questions.md`); `returned`/`sent_back` lifecycle code is left **dormant** for easy restore. **Mine/Flagged are NOT tabs and NOT a standalone toggle/severity dropdown:** all scope filters live in a single **Filters modal** (revised Jun 18 2026 — replaced a popover of nested dropdowns that didn't scale to large data; `QueueFilters` molecule — centered modal on `.ui-modal-backdrop`, staged draft, **Clear all** / **Apply** footer; the modal's `open` is **controlled by the reviews page** so the subhead can reopen it). The **five facets are hybrid by data size**: small fixed enums are **inline toggle pills** (`FacetPills`, reusing `.qf-opt`) — **Findings (Critical/Fail/Flag/Clean, severity color cues) · Type (Technical/Administrative)**; large open-ended sets are a **searchable multi-select dropdown** (`MultiSelectField` — select-styled trigger opens a portal checkbox menu with a search box; menu `z-index: 140` clears the modal backdrop, and Esc closes the menu only, not the modal) — **Reviewer (initials `Avatar` + name, current user tagged "You") · Appraisal firm**. Selections surface as a removable **active-filter chip** strip in a **single-row subhead** (`ActiveFilters` — overflow collapses into a **"+N more"** pill that reopens the modal; how many chips fit is measured off-screen so the row never wraps). **Every facet shares the same tri-state model `MultiSel = "all" | string[]`** (in the hook): `"all"` = default (no filter, **no chip** — the thumb rule: all-selected = default), a **subset** filters to those, `[]` = **none** (matches nothing) → a removable **"No …"** chip. Rendering differs by control: a **pill** shows the **"All …" pill filled** with options neutral (picking one while "all" starts a fresh `[value]`); a **dropdown** shows the **"All" row** as a plain toggle (`"all"` ↔ `[]`), every row checked under `"all"`, indeterminate master for a subset, and a full selection collapses back to `"all"`. To isolate one in a dropdown: click **All** to clear, then check that one. **"Mine" = checking your own name** (no separate toggle); **there is no "severity" filter — it's the honest *Findings* facet** (`findingsKey()`). Filter state: `findings · types · reviewers · firms`, each a `MultiSel`. **No Due facet** (dropped Jun 18 2026 — deadline triage is covered by the sortable **Due column** + its overdue/due-soon urgency markers + the **Needs action** tab; a multi-select Due filter added a facet for marginal value). Toolbar = tabs + search + Filters + Order CTA on one line. **Search scopes the tabs** (Jun 18 2026): tab counts are computed over the search-matched set, and while a query is active **tabs with zero matches are hidden** (All always stays); if the active tab empties, the view falls back to All (derived `effectiveTab`, so clearing the search restores the prior tab). Search match = property / loan# / firm / type (`matchesQuery` in `useReviewQueue`). **Persona/org model:** the org **is the bank** (context in the org card, *not* a row column); per-row parties are the external **appraisal firm** (fee appraiser — the send-back target), the loan, and the assigned **teammate** (queue = team view; signed-in user is `CURRENT_USER.id` → `user-001`).

- **Run flow (J3/J1 takeover) — `components/run/`, brief `../docs/plans/parachute-update-plan.md`.** One app, three entry modes (`session.store` `full`/`embedded`); a **full-page takeover** (`RunModal`) opened from the dashboard `IntakeWidget` (Drop / From-YouConnect) or the YouConnect SSO handoff. Stages cross-fade via `AnimatePresence`, keyed by `run.store` `RunSpoke`: **`confirm → progress → workbook`**. The left rail (Workbook · **Findings**) shows **only in the review** (workbook/exceptions); **Customize & Sign are actions *on* the workbook**, not nav. **Terminology: user-facing label is "Findings" everywhere** — the `exceptions` spoke key + exceptions-first ordering are internal only. **Confirm gate** (`RunConfirm`) = source-aware pre-review step, full-page **no side nav**, one centered card: parsed file chip + auto-filled identity fields (`.field`) + a **Review-type checkbox selector** (Technical locked-on = the built path; Administrative an optional capture for later). Nothing required → one-click **🚀 Start review** (commits `display`/`reviewTypes`, then `progress`); Cancel + top-bar ✕; **embedded close/return routes to `/youconnect`**, standalone to `/reviews`. **Progress** (`RunProgress`) = scanning-doc hero + the real `PIPELINE_STAGES` filling in turn, auto-advances. **Workbook** (`WorkbookPreview`) renders as a real **multi-page PDF**: a branded **cover (navy band + contents list)**, then sections **weight-packed** onto Letter sheets (`estimateWeight` + `PAGE_BUDGET`), running header + "Page X of N" footer, a slim **document toolbar** (zoom + scroll-tracked page indicator), and a corner **Draft** label (no shimmer texture). **Findings** (`RunExceptions`) = an annotated **6-page source appraisal** (`data/appraisal-doc.ts`, built per-review): white pages on dark viewer chrome, **continuous scroll**, firm **letterhead** cover; AI findings are pinned on the doc as inline **severity-tinted highlights + numbered right-margin tags** (measured `offsetTop`) synced to a right-rail **accordion** (rotating chevron, zoned body: meta → analysis → evidence inset → divider → decision bar; `⋯` header menu "Agree with all" = pending only). **Sign** (`RunSignModal`) = floating 2-step seal (type/draw + SHA-256). **`/launch`** = the SSO **handoff/bridge** screen (YouConnect ↔ Parachute as peers + a drifting document chip, stepped checklist, arrival morph), on the **same theme-sensitive canvas as start/login**.
- **Rendered DOCUMENTS use a fixed white `--paper-*` token scale in BOTH themes** (the signable workbook + the source appraisal are deliverables — WYSIWYG with the downloaded/sent PDF; nobody sends a dark-mode PDF). Documents re-scope `--md-*` → `--paper-*` inside `.wb-doc` so existing `.wb-*` rules paint on paper with no per-rule edits; severity/state on paper use the `--paper-ok/-warn/-err/-crit/-info` set. Only the **chrome** around a document (rails, toolbars, footers, the run shell) follows light/dark. The workbook's navy cover band stays navy. (Flagged to Realwired — F-115; reverses the earlier "theme-aware workbook paper" call.)

## Roadmap & status

Current build state, the POC→new-pattern mapping, and what's stubbed / still on old design live in `../docs/plans/parachute-v2-build-plan.md`; per-page early specs (with POC screen refs, critiques, open questions) in `../docs/plans/parachute-v2-early-specs.md`. Read them before building a new page so you match the intended pattern and don't rebuild something already planned. The POC (`../docs/references/client-html-mock/parachute-mockups.html`) is a **reference to reinterpret, not clone** — analyze it, then design with our patterns.

## Before claiming work is done

- Run `npx tsc --noEmit` **and** `npx eslint src --ext .ts,.tsx` — both must be clean.
- Hit the affected routes (expect 200; no runtime/console errors).
- State what was verified honestly. The user does the final **visual** confirmation in the browser.
