# Parachute v2 — Feedback Log

Single running record of every piece of feedback from Cody (boss) and Ed (client), and which build addressed it. **Purpose: stop feedback being "ignored / forgotten."** Add a row when feedback is raised; update Status when addressed; cite the commit/PR or screen.

**Legend — Status:** 🔴 Open · 🟡 In progress · 🟢 Done · ⚪ Deferred (with reason) · 🔵 Needs decision
**Repeat?** = this point was raised before and recurred (these are the highest-priority — they erode trust).

| ID | Raised | Source | Area | Feedback | Repeat? | Status | Addressed in / Notes |
|----|--------|--------|------|----------|---------|--------|----------------------|
| F-001 | Jun 10 | Cody | Process | Come to meetings prepared (~7 min lost finding the Vercel link). | — | 🟢 | Carried as standing rule. |
| F-002 | Jun 10 | Cody | Design | Inputs should have **white backgrounds**. | Yes (repeated at Jun 10) | 🟢 | Standing rule; verify still honored across new screens. |
| F-003 | early (pre-Jun 23) | Cody | Review Details | Review findings **from the context of the PDF, with annotations** — Ashore-style. You can't tell *where* a finding sits in the document otherwise. | **Yes** — re-raised Jun 23 as "ignored or forgotten" | 🟡→🟢 | **Built (Jun 30) in the run flow's Exceptions (S-B):** PDF is the hero — a 6-page source appraisal (white pages, dark viewer chrome, continuous scroll). Findings are pinned **directly on the document**: the cited span is highlighted inline (severity-colored) with a **numbered margin tag** beside it, synced to the thread; select ↔ scroll-and-focus. Modern evolution of the Ashore pin (anchored to real evidence, not free coords). Ref: https://sea.ashoreapp.com |
| F-101 | Jun 23 | Ed/Cody | Strategy | Product has become **"another queue to manage"**; for the 90% user it feels like *more* work than YouConnect's drag-drop-and-forget. | — | 🔵 | Core rework. Lead with 90% use case (upload→workbook); config opt-in after run. Await wireframes (`cody-userflows-mocks.md`). |
| F-102 | Jun 23 | Ed | Strategy | Need **persona-aware views** (job-manager queue vs. 90% "is it done?" vs. supplemental reader). | — | 🔵 | Define personas in IA before screens. |
| F-103 | Jun 23 | Ed | Architecture | Reconsider **property-centric vs. entity (property + report type)**; decouple UI from hardcoded "technical + administrative only." | — | 🔵 | Needs Ed's decision. MVP stays admin + technical. |
| F-104 | Jun 23 | Cody | Process | Wireframes + revised plan "tomorrow afternoon." | — | 🟢 | Delivered as `cody-userflows-mocks.md` (3 user journeys). Now the rework brief. |
| F-105 | Jun 23 | Cody | Auth | Where is the registration flow? Confirm PLG was deprioritized (bank vendor-approval concern). | — | 🔵 | Confirm before building/removing registration surface. |
| F-106 | Jun 23 | Cody | Review List | **"Clean" not "clean"** — capitalize the findings status chip. | — | 🔴 | P2. Fix chip label casing in the table (filter label already "Clean"). |
| F-107 | Jun 23 | Cody | Review List | Findings/Type **filters should be multi-select**. | — | 🟢 | Verified (Jul 2 audit): state is a true multi-value array (`useReviewQueue.ts`), UI is `FacetPills` — independently toggleable pills + "All" reset; each selection surfaces as its own removable chip. |
| F-108 | Jun 23 | Cody | Create Review | **Lead with standalone upload** (90% drag-drop), don't default to "From YouConnect." | **Yes** — "why did you ignore my feedback" | 🟡 | **Decision (Jun 30): make standalone the default/first path; YouConnect secondary.** PARTIAL (Jul 2 audit): Launchpad `IntakeWidget` leads with "Drop a file" ✅; but `OrderModal` Source step still defaults to + lists "From YouConnect" first. Open Q: does the decision extend to the Order (job-manager) flow? |
| F-109 | Jun 23 | Cody | Review Details | Don't love the multi-pane finding UI — wastes screen space; **display a finding in a sheet.** | — | 🔵→🟡 | Part of R1. Held for wireframes. |
| F-110 | Jun 23 | Cody | Review Details | **"Effectively four sidebars"** (nav + list + focus + PDF) — never good. Use a **list that opens a sheet.** | — | 🔵→🟡 | Part of R1. Held for wireframes. |
| F-111 | Jun 23 | Cody | Builder | Same "four sidebars" critique on the Builder — list-opens-a-sheet instead. | — | 🔵→🟡 | R3. Held for wireframes. |
| F-112 | Jun 23 | Cody | Builder | **No realtime Workbook preview** — add a **Preview button** that opens it for review. | — | 🔴 | R4. Can proceed independent of wireframes. |
| F-113 | Jun 23 | Cody | Administrative | **Preview is cut off at the bottom** (content runs under the taskbar). | — | 🟢 | Fixed: both admin preview surfaces use `flex:1; min-height:0; overflow:auto` (`.wb-stage`, `.run-wb-stage`). Verified in code Jul 2. |
| F-114 | Jun 23 | Cody | Templates | Response Templates → Org library **master-detail editor is bad UX** — list that opens a sheet. | **Yes** — "previously… really disappointed it's being ignored" | 🔴 | T1. Apply the list-opens-sheet remedy. |
| F-115 | Jun 30 | Brandcave (self) | Documents | **Rendered documents now use fixed WHITE paper in both app themes** (was theme-aware). The workbook (signable deliverable) and the source appraisal (a real third-party PDF) both render on a shared `--paper-*` token scale; only the surrounding chrome follows light/dark. The workbook cover band stays navy. | — | 🟢 | **Flag to Realwired** — reverses the earlier "theme-aware workbook paper" decision. Rationale: WYSIWYG with the downloaded/sent PDF; nobody sends a dark-mode PDF. Also: run-flow workbook is now a paged multi-sheet doc (cover + contents), and the DRAFT diagonal-stripe texture was removed (label kept). |
| F-116 | Jun 30 | Cody | Run flow / Confirm gate | **Review type must come FIRST, above the property inputs**, and the inputs must be **dependent on the selected review type** ("two of potentially many" types; some not property-based). | **Yes** — "a very big concern brought up in the last call" | 🟢 | **D2. BUILT (Jun 30)** in `RunConfirm.tsx` per spec §10. Type-first single adaptive card: a **horizontal card grid** (2 live — Technical locked-on + Administrative checkbox — + 5 muted "Soon" cards: Evaluation / Vendor short form / Property-type technical / Environmental / Residential), 2-layer selection (card on-state + per-type setup that fades in via AnimatePresence), **lean scope** (property identity + per-type checklist/layout only; assignee/due/priority stay in Order), **generic `ReviewTypeSpec` registry** (`propertyBased` flag + `fieldGroups`; form renders the union). Store widened (`RunReviewType` id-space, `checklistId`). tsc + eslint clean; `/dashboard` 200. **Does not** resolve property-vs-entity (Q3 — registry hedges via `propertyBased`). |
| F-117 | Jun 30 | Cody + Ed | Run flow / Ordering | **One PDF upload, select MULTIPLE review types** in a single order (80% order both technical + admin; never re-upload). Bulk *multi-appraisal* deferred. | — | 🟢 | **D3. BUILT** (commit 31f3a3c): multi-select at confirm (Technical locked + Admin toggle), store carries per-type config (`checklistId`), run renders separate per-type tracks (workbook vs. attestation) with independent signatures; return gated on all types signed. Verified Jul 2. |
| F-118 | Jun 30 | Cody | Findings | **Finding-action language → Accept / Edit / Reject / Remove** (was Agree/Override/Flag; +Comment/Condition/Flag behind ⋯); **changes regenerate the workbook** (not live); add **citations/proof-points** per annotation (compounding-document). Annotations **too small** — enlarge. Highlight-primary for vector PDFs (margin tags = scanned/OCR fallback). | — | 🟡 | **D5/D6/D8 + F3–F5. MOSTLY BUILT** (commit 31f3a3c, verified Jul 2): Accept/Edit/Reject inline + ⋯ menu (Remove/Comment/Condition/Flag) w/ keyboard shortcuts; regenerate-not-live w/ dirty callout; enlarged highlight-primary annotations w/ inline numbered badges; shared `FindingDecisionBar` reused in run flow + routed review; per-type sign gating. **Remaining: citations/proof-points UI (F3) only — blocked awaiting Ed's raw 5-stage output + anonymized PDF.** Plan: `plans/parachute-f118-findings-plan.md`. |
| F-119 | Jun 30 | Cody | Dashboard / Nav | **Drop zone = its own full-page route** from a **primary nav action above Dashboard**; don't fold it into the dashboard (dashboard stays secondary). | **Yes** — Cody's wireframe intent / "design reference I gave you this morning" | 🟢 | **D4. BUILT (Jul 1).** New **`/launchpad`** route (`(shell)/launchpad/page.tsx`) is now the app's landing page — `IntakeWidget` moved off the dashboard and rendered as a full-width, header-less **`hero` variant** (taller drop zone matched to the From-YouConnect state; icon-less "Start a review" header). Nav action **"Launchpad"** added first in the rail (above Dashboard) + command palette + breadcrumb label. Post-login (`/login`) and YC handoff (`/launch`) now land on `/launchpad`; dashboard stays secondary and keeps the "Welcome back" greeting. Addresses Ed's "anxious blank space / no primary CTA." |
| F-120 | Jun 30 | Cody | Workbook | **Cover page and table of contents each their own page** in the paged workbook PDF. | — | 🟢 | **D7. BUILT** (commit de57c54): cover is Page 1 (`wb-page--cover`), contents its own Page 2 (`wb-page--toc`), content starts page 3. Verified Jul 2. |
| F-121 | Jun 30 | Cody | Run flow / Chrome | **No theme switcher reachable in the takeover** (demo ran all in dark); ensure run flow supports dark + light with a reachable switch. **SSO handoff** stepped-completion should be **consistent** with the progress screen's pattern. | — | 🟡 | **X2 DONE** (commit 2e4edc4): `/launch` reuses the `run-st` step rows/animations from the processing screens. **X1 OPEN**: run takeover header (`RunModal.tsx`) has only logo/title/close — no theme toggle reachable. |
| F-122 | Jun 30 | Cody | Process / Infra | **Vercel root directory wrong** (deploy shows empty) — fix and **share link with Ed**. Reach Ed via **Outlook**, not email. | — | 🔴 | **I1/I2.** |
| F-123 | Jul 2 | Cody | Run flow / Confirm gate | **"Bank policy is part of settings — not uploaded every single time."** The Admin setup card asked for a per-run policy upload. | — | 🟢 | **BUILT (Jul 2):** settings-backed `useOrgStore.bankPolicy` (upload/replace/remove lives in **Settings → Compliance**); the confirm gate now shows a **read-only "From Settings" reference row** (same inherited pattern as the workbook layout), with a "no policy on file" empty state. Decision (Val): reference-only, no per-run override. |
| F-124 | Jul 2 | Cody | Run flow / Tabs | **"The dots in the tab don't really make sense to me — what do they represent?"** (The two-type tabs carried an always-on "breathing" status dot: pulse speed = pending vs processing — illegible.) | — | 🟢 | **BUILT (Jul 2):** meaningful-state-only glyphs — small **spinner while that type is processing**, **check when signed**, **nothing while simply pending**. `TypeDot` removed. **Contrast fix (Jul 2, Val):** on the active pill the spinner now uses `--md-on-selected` ink — white was invisible on the light-navy selected pill in light mode. |
| F-125 | Jul 2 | Cody | Attestation doc | **"Why would this be in the workbook?"** — the "0 of 22 items attested — complete the checklist before signing" progress pill was printed ON the document cover. | — | 🟢 | **BUILT (Jul 2):** process status removed from both attestation covers (run-flow `RunAttestationPreview` + review-details `AttestationPreview`); the document keeps facts only (WYSIWYG per F-115). The Sign gate's blocked note carries the "N still need attesting" status. Decision (Val): remove entirely, no chrome relocation. |
| F-126 | Jul 2 | Cody | Launchpad | **"Make the section full height of the DOM. UI here looks bad — look at how I did it for scientist."** The hero was still a card with dead canvas below. | — | 🟢 | **BUILT (Jul 2), REWORKED same day** after Val review vs. the live scientist reference (measured: headline 20px/600, zone 512×205 — ours was 34px + 960×470, and the YC list pushed the toggle below the fold): now a **viewport-locked page** (never grows past the fold), compact centered column — **layered animated emblem** (rotating arc ring + pulsing petrol dot + shimmer), 21px headline, **fixed 640×~300 stage shared by both modes** (YC list scrolls inside; zero height jump), cross-fade between modes, idle bob on the upload glyph, faint radial accent wash. Verified in browser, both themes. **Iterations 3–6 (Jul 2, Val):** hero subtexts finalized as SINGLE lines, no em dashes ("Parachute reads it, runs the review, and compiles your workbook." / "Search and pick one of the N new deliveries to start."); emblem motion settled after several takes (ambient dots read as broken outside the circle; rising dots + float read as submarine bubbles; orbiting satellite felt detached) on **SPEED LINES** — thin petrol streaks passing diagonally top-right → bottom-left BEHIND the static rocket inside the circular badge (a −45° layer, 3 offbeat lanes, peak opacity ≤0.5): the world moves, the rocket flies. **Shimmer removed entirely; presence dot removed** (our AI is a pipeline, not a persona-agent — "online" semantics would be false). Note: dev server needed CSS-flush touches/restart repeatedly this session (stale-CSS failure mode). |
| F-127 | Jul 2 | Cody | Nav | **"I asked you to make the Start a Review a primary action in the main nav."** We shipped "Launchpad" as a plain nav row — a place, not an action. | **Yes** — repeat of D4's "primary nav action" intent | 🟢 | **BUILT (Jul 2):** filled **navy "Start a review" CTA docked above the nav rows** (Gmail-Compose pattern; no active-pill state — the breadcrumb names the page), routes to `/launchpad`; collapsed rail = icon-only + tooltip. "Launchpad" removed from UI language (breadcrumb + palette now "Start a review"; route name unchanged). Decision (Val): keep nav item "Reviews" (no "Review queue" rename). **Realigned (Jul 2, Val):** CTA geometry now mirrors `.ui-navitem` so the rocket sits in the rail's icon column; hairline divider marks the action zone; flat-safe micro-interactions — **no gradient** per the standing "solid fills, no gradient buttons/glows" rule. **Iteration 3 (Jul 2, Val):** press-scale replaced with a **tactile press** (button sinks 1px, shadow flattens) + the rocket nudges on a **framer spring with overshoot** on hover. |
| F-128 | Jul 2 | Val | Run flow / Confirm gate | **Remove "Always included" lock from Technical**; any type can be unticked; **Start gates on ≥1 selected**; a **single selected type shows only that review — no tabs**. | — | 🟢 | **BUILT + browser-verified (Jul 2):** Technical unlocked (no lock tag/registry flag); both live types **pre-checked by default** (Ed: "80% order both" — decision Val); `canStart` requires ≥1 type; zero-state shows an inline note ("Select at least one review type to start."); `RunModal.effectiveType` resolves to the single ordered type (admin-only verified end-to-end: no tab bar, lands straight on the attestation); admin-only runs get **one processing beat** (pipeline progress doubles as admin processing — no second loader; decision Val). AGENTS.md convention line updated. |
| F-129 | Jul 2 | Val | Run flow / Threads | **Grey slab on the expanded accordion item reads dingy in light mode** (attestation checklist + findings thread — shared `.run-ex-item.active`). Also: the **processing spinner was barely visible on the active tab pill**. | — | 🟢 | **FIXED (Jul 2):** open accordion item carries **no fill** — expansion + colored severity bar + rotated chevron are the emphasis (modern accordion convention); hover grey stays on closed rows only (decision Val: white over lighter-wash/navy-tint). Tab spinner now uses `--md-on-selected` ink (white was invisible on the light-navy pill in light mode). |
| F-130 | Jul 2 | Val | Run flow / Two-type | **Admin processing must start automatically** when the review view opens — not wait for the first Admin-tab click. | — | 🟢 | **FIXED (Jul 2):** the 6.2s admin timer now kicks off on arrival at the review view (spoke = workbook/exceptions); the Admin tab's spinner narrates the background run, flipping to ready (or a check once signed) without any tab click. |
| F-131 | Jul 2 | Val | Run flow / Threads | **Rail ↔ annotation navigation broken for some items** in the Admin checklist (and doc→rail in both threads). | — | 🟢 | **FIXED (Jul 2):** root cause — the mock appraisal only carries highlighted spans for 5 of 22 checklist items, and `selectItem` only looked for a span (so 17 items + their "p.N →" evidence buttons navigated nowhere). Now falls back to **scrolling the doc to the item's evidence page** when no span exists; and selection coming **from a doc highlight also scrolls the rail** to the expanding accordion item (after collapse settles). Applied to both the Admin checklist and Technical findings threads. |
| F-132 | Jul 2 | Val | Run flow / Regenerate | **Regenerate workbook/attestation gave no visible feedback** — the doc swapped instantly, reading as live-update (against the D5 model). | — | 🟢 | **BUILT (Jul 2, decision Val: compile sweep on the document):** a fresh Regenerate stamps `regeneratedAt`/`attRegeneratedAt` (explicit-regenerate only, not initial compile); the workbook/attestation preview plays a ~1.2s **recompiling beat** — sheets dim, a skeleton band sweeps each page, a sticky "Folding your decisions/attestations in…" chip narrates — then the fresh doc settles and the dirty callout is gone. Works both from the Findings/Checklist footer and the inline dirty-callout Regenerate. |
| F-133 | Jul 2 | Cody | Run flow / Confirm gate | **"Why would bank policy even be here?"** — it's settings config, not a per-run pick, so it shouldn't appear in the setup gate. Deeper issue Val spotted: the three setup rows used **three different controls** for the same concept (workbook layout locked, checklist an open dropdown, bank policy a read-only "From Settings" row). Need one pattern that **shows the inherited default but lets the reviewer override it just for this review**, and better vocabulary (checklist + workbook layout are both *templates*). | **Yes** — extends F-123 (bank policy is settings-owned) | 🟢 | **BUILT + browser-verified (Jul 2):** conceptual line drawn — **templates (per-review overridable) vs. org policy (global, not overridable)**. **Bank policy removed entirely** from the gate (Settings → Compliance owns it; reverses F-123's reference-row). New shared **`InheritedTemplateField`** control (the "inherited value + per-instance override" pattern). Applied to **both** Workbook layout (Technical) and Compliance checklist (Administrative), each staying inside its own **type-gated section** (a Technical-only run never renders Admin config — decision Val). Layout override is real: threaded `layoutId` through `run.store` → `RunModal` seeds the workbook from the chosen layout. `value===null` = inherited, non-null = audited override. **Iteration 2 (Jul 2, Cody/Val):** the first cut *swapped* the tile into a bare `<select>` with an orphaned "Reset" link — read as broken. Reworked to a **stable tile + popover**: the tile never dissolves; a **`Change ▾` pill** opens the app's existing **`ActionMenu`** popover (extended with an optional custom-trigger render-prop + `menuClassName`, backward-compatible) listing templates with a check on the current, `(default)` marked, and a **"Reset to default" footer shown only when overridden**; picking updates the tile in place (meta → "For this review only"). tsc + eslint clean; verified end-to-end in browser (open → override → reset). Decisions (Val): popover selection; reset in popover footer; meta-line override cue; remove bank policy entirely; unify both. |
| F-138 | Jul 7 | Cody | Run flow / Confirm gate | **"'What do you want to review?' is unclear — we're selecting a review type. Each of these could use a description."** | — | 🟢 | **BUILT (Jul 7):** heading → **"Review type"** (matches sibling card heads) + helper line *"Select one or more — each adds its own setup below."*; each type row restructured to a **stacked label + one-line description** (output-first, mirroring the Order flow's Configure). Descriptions added to the "coming soon" types too. New `.run-cf-card-sub` / `.run-cf-opt-body` / `.run-cf-opt-desc` (with selected + soon tones); rows now `align-items: flex-start`. `RunConfirm.tsx` + `globals.css`. Decisions (Val, MCQ): heading + helper, stacked-no-icon rows. tsc + eslint clean. |
| F-139 | Jul 7 | Cody | Launchpad / Intake hero | **"An order might not be property-based — review language needs to be more generic (no 'appraisal')."** | **Yes** — extends F-116 / Q3 (property-vs-entity) | 🟢 | **BUILT (Jul 7):** hero title **"Drop an appraisal to start" → "Drop a report to start"** ("report" = domain-native — Ed's "report type" — and true for appraisals, evaluations, and non-property vendor forms). Review-type descriptions also use generic **"the report's"**. **Scope (decision Val, MCQ):** hero + review-type copy only; deeper property-specific strings (parsing subtext "Extracting property, lender and loan details.", default `Appraisal.pdf`, "Property details" card) left as-is — accurate for the appraisal-only MVP, real fix blocked on **Q3**. `IntakeWidget.tsx` + `RunConfirm.tsx`. |
| F-134 | Jul 2 | Cody + Val | Iconography / Brand | **The rocket icon (used for run/start/order in ~11 spots) is off-brand** — Parachute's metaphor is descent / safe-landing / gliding, not launch; the rocket also reads as an AI-slop cliché. Move to a parachute across all entry points, and rework the launchpad emblem animation to match (glide, not blast-off). | — | 🟢 | **BUILT + browser-verified (Jul 2), prototype-driven.** Built a local **git-ignored** prototype (`/prototype-icons/`, kept for reference) comparing rocket vs line parachute / filled parachute / hand-drawn **paraglider** / filled brand canopy / **logomark-as-canopy**, across every UI element + a live emblem-animation gallery, both themes. Decisions (Val): **line `parachute` (Tabler) on all action spots** — nav CTA, review-row "Run", RunConfirm "Start review", Order submit, dashboard "Run" widget + "Pipeline running" KPI, command palette, `/start` tile, Technical + Admin workspace empty-state CTAs; **AI cue unchanged** (filled brand canopy stays `name="ai"`); **hero emblem = logomark-as-canopy** — new `ChuteEmblem` (BrandGlyph.tsx): the real logomark paths as the canopy + two thin shrouds + a payload box. **Emblem motion (framer-motion):** gentle ±4.5° sway + a bob, with soft **clouds drifting up** behind it (clipped to the seal ring) — the old rocket + diagonal speed-lines removed; sideways-cloud direction (prototype) set R→L. Nav CTA hover retuned from a rocket "launch-nudge" to a parachute "catch-air" dip+sway. tsc + eslint clean; emblem + CTA verified animating in browser. |

| F-140 | Jul 7 | Val | Run flow / Admin checklist | **In the run-flow Administrative Checklist: (1) the right-rail header read "Attestations" while the nav tab (and the surface) is "Checklist" — inconsistent; (2) most checklist items (e.g. display #1, #2, #16) didn't navigate when selected, and the "p.N" citations showed page numbers not present in the 6-page doc.** | — | 🟢 | **BUILT + tsc/eslint clean (Jul 7):** **(1)** rail title → **"Checklist"** (`RunAttestations.tsx`), restoring the Technical parallel (nav tab == rail title). **(2) Root cause:** the seed `page` field carries the *original full-length report's* pagination (up to 72), but the run-flow proofing doc renders only **6 pages**; navigation is span-first with a `goPage(page)` fallback, and only **5 of 22** attestations had on-page anchor spans — the other 17 fell back to non-existent pages → dead-ends, and the "p.61/p.72" chips read as "random." **Fix:** anchored **all 22 checklist items + kept all 7 findings** to real spans in `appraisal-doc.ts` (co-anchoring shared spans on both tracks where the same sentence is cited), added a **`docPageIndex(doc)`** helper that maps each anchor → its *rendered* page, and switched both run-flow viewers (`RunAttestations` + `RunExceptions`, per the "align both tracks" decision) to cite/navigate by that rendered page instead of the seed `page`. Scrubbed inline "(p.NN)" parentheticals from the shared seed prose (findings + attestations) → section references, so nothing contradicts the derived page on either surface. **Seed `page` left untouched** (the separate review-details `source-pages` model still consumes it). Decisions (Val, MCQ): anchor-all + truthful pages; align both tracks; rename to "Checklist". **⚠ Follow-up flagged:** the **review-details** Administrative workspace (`/reviews/[id]`, `AdministrativeWorkspace`) has a *parallel* latent bug — it navigates by seed `page` into `source-pages.ts` (keys {8,12,33,47,52,58,60,61,64}), and most attestation pages aren't valid keys, so those items dead-end there too. Not fixed in this pass (different surface, needs source-page authoring); flag for a follow-up. |

| F-141 | Jul 7 | Val | Run flow / Findings + Checklist | **On landing in Findings and Checklist, the first item is auto-opened and its span styled active, but the source document stays parked at the cover — the highlight isn't shown until you first click the item. The open item ↔ active highlight ↔ visible page didn't agree on arrival.** | — | 🟢 | **BUILT + tsc/eslint clean (Jul 7):** both run-flow viewers now perform a **one-shot landing scroll** to the first item's annotation on mount (`requestAnimationFrame`, `behavior: "auto"` instant jump — the view arrives via a cross-fade, so an animated scroll would fight it; falls back to the item's derived page when it has no on-page span). A `didInitialScroll` ref guards it to once per mount. `RunAttestations.tsx` + `RunExceptions.tsx`. Decision (Val, MCQ): scroll to first item on landing (vs. don't-pre-select / leave-as-is). |

| F-142 | Jul 7 | Val | Findings + Checklist / Decision actions | **Clarify the finding decision actions and fix two model problems: (1) Comment was treated as a disposition, so you couldn't Accept-and-comment; (2) Edit ("override") didn't actually rewrite the finding — it stored a separate rationale, mismatching intent + the composer copy.** Also asked whether the Checklist rail needs an AI audit trail. | — | 🟢 | **BUILT + tsc/eslint clean (Jul 7):** **(1) Edit rewrites the finding.** Renamed disposition `override` → **`edited`**; the composer's edit mode now saves the reviewer's wording, which **replaces the AI analysis** shown in the findings rail (`RunExceptions`), the routed focus pane (`FindingFocus`), and the workbook (`dispositionLine` → the edited text), each tagged "Rewritten by reviewer." **(2) Comment decoupled from disposition.** Removed `commented` from the `Disposition` union; the Comment action now saves an **independent note** (`FindingState.comment`) that can accompany any decision — surfaced as a "Comment" pip on the decision bar, a note line in the run-flow decision zone, and a "Reviewer note:" line in the workbook. **(3) Checklist rail stays lighter** — no multi-stage audit trail (evidence + confidence + required divergence-reason remain the attestation basis); decision Val, MCQ. Touched: `domain.types` (Disposition/FindingState docs), `workspace.store` (tally), `findings.seed` (suggestedDisposition), `lib/utils` (DISP_META), `lib/workbook` (DISP_TAG/dispositionLine), `FindingDecisionBar`, `ResponseComposer` (modes edit/comment), `RunExceptions`, `FindingFocus`, `FindingList`, `WorkbookPreview`, `Builder`/`RunCustomize` labels, `globals.css` (new pip/edited/comment classes). Kept: the per-order "override" template + `hideOverridden` setting key (unrelated concept / internal key). Decisions (Val, MCQ): Edit = rewrite; Comment = independent note; Checklist lighter. |

| F-143 | Jul 7 | Jeff (SME) + Ed | Strategy / Workbook | **Findings merge into the workbook — the workbook is the decision surface.** Inline Concur/Edit/Reject/Delete on finding blocks in the document; the annotate-the-source Findings flow retires as the decision surface. **Source appraisal = read-only truth** the workbook cites; never disputed/annotated. ("The findings go away, the findings get merged into the workbook.") | — | 🔴 | **Direction change (supersedes the decide-in-Findings model of F-118).** Plan: `plans/parachute-inline-workbook-plan.md` §6.1–6.3. Decision (Val, MCQ): existing Findings surface survives as read-only **"Source"** tab (peek-drawer alternative noted for build time). |
| F-144 | Jul 7 | Jeff + Ed + Cody | Workbook editor | **Inline HubSpot-builder-style editing of the workbook**: hover/select a section → few, visible, **section-specific** tools — edit text in place (WYSIWYG, no raw markdown), delete/hide/reorder/add sections, **repeaters get add/delete/edit row** (Loom: comp grid, "delete Comparable 5"). Explicitly NOT formatting tools; NOT a settings-grid side panel ("select the box and get the grid on the left — way too much"). | — | 🔴 | **GA-blocking (Ed: "This doesn't work if we don't have the inline").** Decisions (Val, MCQ): **block-based architecture** (typed blocks, per-type tools — not a whole-doc markdown mode-flip); **direct edits live** (compile beat only for system recompute — supersedes Jun-30 D5 for direct edits). Plan §4–5. Refs: `references/hubspot-builder -references/`. |
| F-145 | Jul 7 | Cody (proposed) + Jeff (confirmed) | Findings model | **User-created annotations/findings + inline comments** — AI won't catch everything; reviewer-added findings surface in the workbook (tagged "Reviewer-added"), comments attachable to any cell/section (hover tooltip button). **Every action audit-trailed** — regulator must see the human touched it. | — | 🔴 | Plan §6.2. Open Qs for client: do reviewer-added findings need citations; delete = excluded-but-audited vs gone. |
| F-146 | Jul 7 | Jeff | Customize rail | **Customization rail = the 20%** — reads as "HubSpot, CRO kind of feel"; hide from the primary flow and from demos; keep reachable for power users. | **Yes** — same overwhelm Ed flagged Jun 30 ("feels like they are building something") | 🔴 | Decision (Val, MCQ): rail closed by default behind a single **`Customize ▸`** toolbar button (dockable panel, current `RunCustomize`). Day-one item of the prototype week. Plan §6.1. |
| F-147 | Jul 7 | Jeff (relaying customers) | Templates | **"Save as my template"** — users won't redo the same manipulations every run ("we don't want to keep manipulating your output… the same edits every time"); one-time save → next run comes out their way. | — | 🔴 | Captures **structure + theme only** (content stays per-review) into the existing versioned `WorkbookLayout`; surfaces via the F-133 inherited-template tile. Open Q: user-level vs org-level shelf. Plan §6.4. |
| F-148 | Jul 7 | Ed + Cody | Process / Roadmap | **Inline editing is GA-critical — no phase-split** (a customer is holding a contract on it). This week: **down-and-dirty prototype** for Jeff to feel the speed ("even if it's super ugly and doesn't even work altogether") + **async Loom-style video updates** instead of waiting for calls; spike extra hours this week. | — | 🔴 | Prototype scope: plan §9 Phase 2a (rail behind button, inline finding decisions, comp-grid row tools, prose edit-in-place, Loom by ~Thu). "Make a plan, send it to me" → `plans/parachute-inline-workbook-plan.md` + `-brief.md`. |

| F-149 | Jul 8 | Cody | Findings / Source view | **"Findings would be both in the document and the Source view. You should be able to edit them from either place. Use HubSpot's builder as a design reference."** (After reviewing the inline-workbook plan.) Amends F-143's read-only-Source cut. | — | 🔴 | **Plan updated (Jul 8):** findings = one shared store rendered in both views with **full action parity** (Concur/Edit/Reject/Delete/comment/flag); the source *document* stays read-only; **create-finding-from-span now in scope** on Source. Decisions (Val, MCQ): tab stays **"Source"**; full parity; **one shared finding editor anchored in place** (element flips to edit mode where it stands — HubSpot's click-to-edit canvas mechanic, explicitly rejecting its side inspector panel per Jeff's "CRO feel" / Ed's "grid on the left" objections). Plan §2-C, §5, §6.3, §8; wireframes updated. |

## How to use this log
- **Every** Cody/Ed note gets a row, same day it's raised.
- Mark **Repeat?** when something recurs — those jump the queue.
- When you ship, change Status to 🟢 and cite the commit/PR/screen in the last column.
- Review this list before each demo so nothing ships still-open silently.
